import React from 'react'
import { render, screen } from '@testing-library/react'
import Home from '../pages/index'

describe('Home', () => {
    it("navigates from products page to product detail page", () => {
        // const { } = render(Home)
    })
})